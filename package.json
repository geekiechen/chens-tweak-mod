{
    "name": "chens-tweak-mod",
    "version": "0.12.2",
    "scripts": {
        "release": "standard-version",
        "full-release": "npm run release && git push --follow-tags && npx github-release-from-changelog"
    },
    "devDependencies": {
        "github-release-from-changelog": "^2.1.1",
        "standard-version": "^9.5.0"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/geekiechen/chens-tweak-mod.git"
    }
}
